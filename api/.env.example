# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cb5_db"

# Admin authentication key — change this to a strong random value in production
ADMIN_API_KEY=change_me_to_a_strong_secret

# Server port
PORT=3000

# Frontend URL for CORS (defaults to http://localhost:5173)
FRONTEND_URL=http://localhost:5173

# Render deployment URLs (set automatically on Render)
# STAGING_URL=
# RENDER_EXTERNAL_URL=
